import React from "react";

function User(props) { //Puedo hacer en vez de props, {nombre, avatar,rol} y aclaro que propiedades espera recibir//
    return (
        <div className="user">
            
        </div>
    );
};

export default User;